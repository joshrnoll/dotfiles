#!/bin/bash

APP_NAME="$2"
URL="$1"

if [ -n "$APP_NAME" ]; then
    exec setsid brave --new-window --app="$URL" --class="$APP_NAME" "${@:3}"
else
    exec setsid brave --new-window --app="$URL" "${@:3}"
fi
